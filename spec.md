# FieldFlow Local 第二轮改进规格

## 目标

以 `pro-plan.md` 对提交 `73fac30` 的复审为输入，逐项核对真实代码和运行结果。确认存在的问题要修复并补回归测试；属于长期产品方向或与已确认语义冲突的建议，要说明边界，不能伪装成已完成。

本轮首先完成业务一致性闭环：工单作为持久业务事实独立于求解结果，计划发布仍保持原子、可验证、可追溯。

## 范围

### 必须完成

- 紧急工单先独立入库，再尝试重排；重排失败时工单和最后发布计划都保留。
- 为当前计划增加覆盖状态，区分完整覆盖、业务数据已变化和新增需求未覆盖。
- 拆清历史计划激活、从历史快照克隆场景、显式回滚业务数据三种操作；兼容旧恢复接口，但不再让含糊按钮静默回滚。
- 重排器和发布验证器共享一份 PlanningContext；时间推断不能冒充真实执行状态。
- 统一重算 `travel_minutes`、`sla_late_minutes`、`changed`、`locked` 和解释等派生字段，业务评分只使用重算结果。
- Candidate 写入和 Run 完成在同一事务中提交；失败和重启恢复不留下矛盾血缘。
- baseline、optimize、replan、历史操作和实验发布使用按场景与动作隔离的幂等语义。
- 输入字符串先去空白再校验；空白名称和标签稳定返回 422。
- 方案比较携带快照可比性、增删改工单摘要，不同需求快照不直接给出优劣结论。
- 调度、指标、解释和验证使用同一个可注入 TravelTimeProvider。
- 同步维护 API、前端状态、架构文档和迁移说明。

### 第二阶段择优完成

- 应用工厂和 Store 生命周期隔离：导入不创建数据库或线程，顺序启动的 app 可注入不同 Store。同进程并发服务多个 app 不在当前保证内。
- 策略实验增加 profile 数量上限、有界队列、协作取消、部分成功状态和显式获胜者。
- SQLite 容量检查与安全的 dry-run 清理入口。
- 本地 Host/Origin 防护和报告文件名清洗；不在缺少迁移方案时强行加入会破坏现有 API 的启动令牌。
- 扩展现场执行动作，但只有在状态、事件、验证、UI 和迁移能同时闭环时才落地。

## 非目标

- 不引入 Redis、云服务、在线地图或外部 AI API。
- 不在本轮实现手机端、完整技师 App、拖拽排程、库存、团队工单、Monte Carlo 或财务成本模型。
- 不擅自选择 LICENSE。许可证属于仓库所有者的法律决定。
- 不把合成 Fixture 描述为生产 Benchmark。
- 不为了符合任务书而拆出空目录、占位接口或未被业务使用的抽象。

## 关键设计决策

### 工单与计划

工单接收事务先提交 D 修订，并将最后发布计划标为 `PARTIAL_NEW_DEMAND`。随后创建重排 Run。求解失败时，D 修订和工单不回滚；最后发布的 V 仍可查看和执行其原覆盖范围。只有经过标准化与验证的完整候选才能发布新 V，并把覆盖状态恢复为 `CURRENT_AND_COMPLETE`。

### 历史操作

- 激活：只允许历史计划与当前业务快照完全一致，不修改工单或技师。
- 克隆：从历史快照创建新的场景 ID，不修改原场景。
- 回滚：显式的数据回滚操作，先返回差异摘要，提交时要求确认令牌和原因；默认禁止重新打开已完成工单或删除新增工单。

旧 `/restore` 路由保留兼容，但语义和界面必须明确写成“回滚业务快照”，不能再以普通“恢复方案”呈现。

### 重排与执行事实

本轮不把计划出发时间当作真实在途事实。自动冻结只依据明确的执行状态和 PlanningContext；基于时间的判断最多生成警告。未来的 `EN_ROUTE` 等状态必须通过执行动作和事件记录进入，而不是由计划反推。

### 候选标准化

求解器只决定工单、技师、顺序和时间。FieldFlow 根据场景、来源计划、PlanningContext 和旅行模型统一生成派生字段；Verifier 检查标准化结果，发布事务再次验证。

## 兼容与迁移约束

- SQLite 迁移前继续生成时间戳备份。
- 现有 V、D、Run、Candidate 和实验记录不能静默删除。
- 旧 JSON 载荷缺少新字段时使用保守默认值并在下一次写入时升级。
- 旧同步接口在过渡期继续工作；新增异步或动作接口不能让现有前端立即失效。
- 普通 optimize/replan 暂保留同步契约。未来的硬取消需要子进程 worker，不能只增加未被执行器使用的状态。

## 验收标准

- `pro-plan.md` 的每个 P0/P1 项都有“确认、部分确认、反驳或延期”的代码证据。
- 紧急工单在无解、超时、候选校验失败和进程重启后仍可查询。
- 失败重排不新增 V，不替换最后发布计划，并显示部分覆盖。
- 伪造 `changed`、`locked`、KPI 或解释不能改变最终候选与业务评分。
- Run 和 Candidate 不出现一方完成、一方仍运行的半状态。
- 相同幂等键和相同请求返回同一资源；相同键不同请求返回 409。
- 不同快照比较明确列出需求差异，UI 不显示误导性的目标值改善百分比。
- 完成至少两轮“发现—修复—重新验证”，并通过 Ruff、TypeScript、后端测试、前端测试、生产构建和 Demo 检查。
- Playwright 若受本机沙箱限制，必须由已有 CI 或等价可复现环境补足，不能把未运行写成通过。
