# FieldFlow Local 可靠性规格

## 目标

FieldFlow 是离线现场服务调度台。系统必须把业务数据、现场执行事实、求解候选和正式方案分开保存；求解失败、重复请求或进程中断不能改写最后一份可执行方案。

本轮以 `pro-plan.md` 对提交 `ea98e33` 的静态审查为输入，并以当前代码、数据库迁移和实际流程复核。审查意见不是需求清单：能在现有单机架构内证实的问题直接修复；需要 Crew、库存或持久 worker 等新领域模型的建议明确保留边界。

## 当前范围

- FastAPI、SQLite、React，单机离线运行。
- 每个场景独立维护 D 数据修订和连续的 V 公开方案版本。
- baseline、optimize、replan、历史激活、业务回滚和实验发布都经过 Candidate 校验及事务内复核。
- 现场状态只能通过“开始服务”和“完成服务”命令变更，不能通过通用工单编辑绕过正式分配。
- 策略实验使用冻结快照，候选不占用 V，人工选择后才发布。
- 行程、指标、解释和发布验证使用同一 TravelTimeProvider。
- 公开 V 的冻结快照可做整数分成本分析、六类容量 what-if 和固定 seed 风险仿真；这些分析不修改 D、V 或正式排程。

## 业务规则

### 正式方案与硬承诺

- 锁定工单必须分配给指定技师；锁定工单缺失或未分配时，整个候选不可发布。
- 服务中的工单必须保留来源 Booking 的技师、计划时间、来源序号和分配指纹。未来路线重新从 1 连续编号，来源序号单独保存，不把历史序号误当成未来队列序号。
- 已完成工单保留来源和执行事件，但不进入未来排程。
- 完成后可连续执行第二次、第三次重排；已完成前缀不要求继续存在于当前 future plan。
- Candidate 一经保存不可修改；发布时再次核对 Run、Candidate、来源 V、场景快照、执行事件水位和完整求解政策指纹。

### 现场执行

- 开始服务要求当前有正式方案、工单有分配、技师匹配、D 修订匹配，并携带独立于重排时点的实际发生时间和幂等键。
- 早于客户允许时间开始时必须填写授权原因；完成时间必须严格晚于实际开始时间，零时长服务无效。
- 完成服务只能发生在开始服务之后，且技师和 Booking 必须与开始记录一致；事件保存计划时间、实际时长、迟到分钟和备注。
- 同一技师不能并行开始两个工单，且必须按正式路线顺序执行；实际开始时间不得早于前序完成加行程时间。
- 前序已经完成时，下一段行程从实际完工位置和时间校验。正在服务但超过计划完成时间时，统一按保守剩余 15 分钟投影，调度器、Verifier 和开始门禁使用同一规则。
- pending 分配保存 planning fingerprint；技能、位置、时长、时间窗、班次、锁定或行程模型变化后，旧分配不能继续开工。姓名、备注等非计划字段不误伤执行。
- 每次动作写入带场景内单调序号的不可覆盖执行事件，并产生新的 D；当前 V 继续作为重排来源。
- 通用工单 PUT 只能编辑资料和备注，不能改变执行状态。

### 时间与行程

- 最早服务时间为 `max(window_start, reported_at)`。
- 加班按末单完成后返回技师出发点的时间计算，KPI、证据和界面说明使用同一口径。
- 行程记录同时保存模型名称和配置/矩阵内容指纹；同名但内容不同的行程模型不能通过发布复核。
- OR-Tools 只有明确返回超时状态时才标记 `TIME_LIMIT_*`；部分局部搜索统一标为可行解，不猜测停止原因。

### 幂等与失败保护

- 突发工单接收和某次重排尝试使用独立幂等记录。重排参数变化可使用新的尝试键，不会重复接收工单。
- 相同键和相同请求返回同一结果；相同键对应不同请求返回 409。
- 同一幂等求解命令在计算前原子抢占；并发重复请求不能重复运行求解器，终态命令不能被晚到状态覆盖。
- 应用重启时，未完成且没有发布结果的 RUNNING 命令转为 `FAILED_RETRYABLE`，相同键可重新获取；已存在正式发布结果的命令对账为完成。
- 失败候选不新增 V，不替换最后正式方案。
- 策略实验取消请求优先于 worker 的陈旧进度写入；当前仍是协作取消，不承诺中断正在运行的 OR-Tools 调用。

## 经营决策

- 成本模型只使用整数分。人工成本按计划占用分钟计；行程成本、加班溢价、SLA 迟到损失、未服务收入损失和外包成本分别列示。
- 容量规划统一使用 `DETERMINISTIC_GREEDY_WHAT_IF_V1` 比较增加技师、补充技能、延长班次、增加加班、外包未服务单和新增服务站点，避免把不同求解算法的波动当成容量收益。
- 风险仿真在冻结 V 上模拟旅行延误、服务时长波动、技师缺勤、突发工单和客户不在场。相同快照、参数和 seed 必须返回相同结果。
- 风险输出包括预计 SLA、迟到 P50/P90/P95、预计加班、预计未服务数和计划失效概率。它是决策估算，不是财务结算或正式承诺。

## 兼容和迁移

- Schema v7 为执行事件增加场景内序号；Schema v8 把旧数据语义修正从读取路径移到一次性迁移；Schema v9 为执行事件序号增加数据库唯一约束；Schema v10 把技师浮点成本按原值换算为整数分，并同步当前与历史快照。
- v1 旧方案历史缺少完整业务快照，继续按已确认的产品决定先备份再重建；不会静默删除当前业务数据。
- 旧同步排程接口继续可用。完整后台队列和进程级硬取消不在本轮范围。

## 非目标

- 在线地图、云服务、手机端技师应用、财务结算。
- Crew 同时到场、零件库存约束、跨规划日排班和完整站点库存模型；当前“服务站点”what-if 只调整一名高行程技师的出发点并明确展示假设。
- 用尚未接入业务的 Booking、Outbox 或 worker 目录充当占位实现。
- 擅自选择开源许可证。

## 验收

- 锁定、技能、时间窗、班次、返程、执行中冻结和覆盖完整性由发布验证器检查。
- `reported_at` 在贪心基线和 OR-Tools 路径都生效。
- 执行动作覆盖错误技师、陈旧 D、乱序完成和幂等重试。
- 连续完成后重排、非首项 started 重编号、过期 pending 门禁、实际位置首段和服务超时投影有确定性回归。
- 行程内容指纹、Candidate 不可变性、来源谱系和取消竞态有回归测试。
- SolverPolicy 区分 original/effective penalty，baseline 不冒充 routing limit，Run 与政策时限交叉校验。
- 成本总额可逐项对账，固定 seed 风险结果完全一致，六类容量方案共用评估方法且不占用 V。
- Ruff、TypeScript 严格检查、后端测试、React 测试、生产构建、Demo、Benchmark smoke 和 Playwright 主流程通过后才能推送。
